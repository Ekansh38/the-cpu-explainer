<Context>
You receive a selection in neovim that you need to replace with new code.
The selection's contents may contain notes, incorporate the notes every time if there are some.
consider the context of the selection and what you are suppose to be implementing
<SELECTION_LOCATION>
range(point(7,1),point(7,110))
</SELECTION_LOCATION>
<SELECTION_CONTENT>
`MUDDY` is closed and `STINKY` is open, but current can flow through the circuit. That is why the bulb is on.
</SELECTION_CONTENT>
<SURROUNDING_CONTEXT>
title: or gate logical

[frame 1]
`STINKY` & `MUDDY` are both open, thus no current can flow. That is why the bulb is off.

[frame 2]
`MUDDY` is closed and `STINKY` is open, but current can flow through the circuit. That is why the bulb is on.

[frame 3]
`STINKY` & `MUDDY` are both open, thus no current can flow. That is why the bulb is off.

[frame 4]
asd

[frame 5]
Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium.

[frame 6]
Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur.
</SURROUNDING_CONTEXT>

</Context>
<Prompt>
reword this in a nice way please
</Prompt>

<Location><File>/Users/ekanshgoenka/Desktop/Projects/the-cpu-explainer/assets/final/or-gate-logical.frames.txt</File><Function>range(point(7,1),point(7,110))</Function></Location>
<FunctionText>`MUDDY` is closed and `STINKY` is open, but current can flow through the circuit. That is why the bulb is on.</FunctionText>
<TEMP_FILE>./tmp/99-6185</TEMP_FILE>
<MustObey>
NEVER alter any file other than TEMP_FILE.
never provide the requested changes as conversational output. Return only the code.
ONLY provide requested changes by writing the change to TEMP_FILE

never attempt to read TEMP_FILE.
It is purely for output.
Previous contents, which may not exist, can be written over without worry
After writing TEMP_FILE once you should be done.  Be done and end the session.

</MustObey>