title: electronic and gate

[frame 1]
`input 1` & `input 2` are both open, so neither electromagnet is powered and both metal arms in the main loop stay open. That is why the bulb is off.

[frame 2]
`input 1` is closed, so its electromagnet starts to pull the first arm. But `input 2` is still open, and the first metal arm hasn't been pulled down, so the main loop is still broken. That is why the bulb is off.

[frame 3]
`input 1` is closed, and the electromagnet has pulled the first arm down. But `input 2` is still open, so the main loop is still broken. That is why the bulb is off.

[frame 4]
`input 1` has just reopened, the electromagnet is releasing the first arm. But `input 2` is still open, so the main loop is still broken. That is why the bulb is off.

[frame 5]
`input 1` & `input 2` are both open, so neither electromagnet is powered and both metal arms in the main loop stay open. That is why the bulb is off.

[frame 6]
`input 2` is closed, so its electromagnet starts to pull the second arm. But `input 1` is still open, and the second metal arm hasn't been pulled down, so the main loop is still broken. That is why the bulb is off.

[frame 7]
`input 2` is closed, thus the electromagnet has pulled the second arm down. But `input 1` is still open, so the main loop is still broken. That is why the bulb is off.

[frame 8]
`input 2` is closed, thus the electromagnet has pulled the second arm down. `input 1` is still closed, so the electromagnet starts to pull the first arm, thus the main loop is still broken. That is why the bulb is off.

[frame 9]
`input 1` & `input 2` are both closed, with both electromagnets powered and both metal arms pulled down. The main loop is now closed. That is why the bulb is on.
