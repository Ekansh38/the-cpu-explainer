title: or gate logical

[frame 1]
`STINKY` & `MUDDY` are both open, thus no current can flow. That is why the bulb is off.

[frame 2]
`MUDDY` is closed and `STINKY` is open, thus current still flows through the circuit, so the bulb stays on.

[frame 3]
`STINKY` & `MUDDY` are both open, thus no current can flow. That is why the bulb is off.

[frame 4]
`STINKY` is closed and `MUDDY` is open, thus current still flows through the circuit, so the bulb stays on.

[frame 5]
`STINKY` & `MUDDY` are both open, thus no current can flow. That is why the bulb is off.

[frame 6]
Both `STINKY` & `MUDDY` are both closed, thus current can flow. That is why the bulb is on.
